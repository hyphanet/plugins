allprojects {
    group = 'org.freenetproject.plugins'
    tasks.withType(Jar) {
        preserveFileTimestamps = false
        reproducibleFileOrder = true
        duplicatesStrategy = "exclude"
    }
}
